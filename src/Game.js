import React from 'react';
import GameBoard from 'GameBoard';

class Game extends React.Component {
    render() {
        return (
            <div>
                <GameBoard />
            </div>
        );
    }
}

export default Game;